package com.hyperdroid.model

enum class PermissionStatus {
    GRANTED,
    NEEDS_DEVELOPER_MODE,
    NEEDS_WIRELESS_DEBUG,
    NEEDS_SHIZUKU,
    NEEDS_SHIZUKU_START,
    NEEDS_VM_PERMISSION,
    AVF_NOT_SUPPORTED
}
